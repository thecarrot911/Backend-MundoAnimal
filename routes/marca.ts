import { Router } from "express";
import * as Marca from "../controllers/marca"
const router = Router();

router.get('/',);
router.get('/:id');
router.post('/', );
router.put('/:id',);
router.delete('/:id',);
export default router;