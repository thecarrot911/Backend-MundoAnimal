import { Router } from "express";
import * as Categoria from "../controllers/categoria";
const router = Router();

router.get('/',);
router.get('/:id');
router.post('/', );
router.put('/:id',);
router.delete('/:id',);
export default router;